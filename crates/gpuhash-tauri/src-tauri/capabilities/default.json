{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — gpuhash commands + event streaming only.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
